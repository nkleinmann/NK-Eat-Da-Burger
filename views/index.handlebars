<header class="header">
  <h1 class="text-center">Burgers!</h1>
</header>


<div class="body">
<section class="container-fluid">
  <div class="row">
    <section class="col-12">
      <h2 class="d-flex justify-content-center">Delicious Burgers!</h2>
    </section>
  </div>
</section>

{{!-- <div class="row">

    <section class="col-12">
      <ul>
        {{#each burgers}}
        {{#unless devoured}}
        {{> burger-block devoured=false}}
        {{/unless}}
        {{/each}}
      </ul>
    </section>

  </div> --}}

</div>
  

  

  {{!-- <h2>Burgers that are devoured!</h2>

<ul>
  {{#each burger}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Devoured?</label><br>
    <input type="radio" name="devour" value="1" checked> Devoured!<br>
    <input type="radio" name="devour" value="0"> Waiting to be devoured
  </div>

  <button type="submit">Add Burger</button>
</form>  --}}






